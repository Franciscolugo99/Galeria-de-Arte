Options -Indexes

<IfModule mod_headers.c>
  Header always set X-Robots-Tag "noindex, nofollow"
</IfModule>

<FilesMatch "^(bootstrap|config|config\.local|config\.example)\.php$">
  Require all denied
</FilesMatch>
